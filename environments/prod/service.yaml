apiVersion: v1
kind: Service
metadata:
  name: demo-nginx-prod
  namespace: demo-prod
spec:
  selector:
    app: demo-nginx-prod
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
