apiVersion: v1
kind: Service
metadata:
  name: demo-nginx-dev
  namespace: demo-dev
spec:
  selector:
    app: demo-nginx-dev
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP